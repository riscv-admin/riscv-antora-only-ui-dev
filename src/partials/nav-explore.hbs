<div class="context">
  <span class="title">{{#if (or (eq page.component.name "hardware") (eq page.component.name "aia") (eq page.component.name "iommu") (eq page.component.name "plic") (eq page.component.name "soc"))}}Non-ISA Hardware{{else if (or (eq page.component.name "debug-trace-ras") (eq page.component.name "techtrace") (eq page.component.name "qos"))}}Debug, Trace, RAS{{else if (or (eq page.component.name "platform-software") (eq page.component.name "ffh") (eq page.component.name "semihost") (eq page.component.name "brs") (eq page.component.name "rimt") (eq page.component.name "sbi") (eq page.component.name "rpmi") (eq page.component.name "uefi"))}}Platform Software{{else}}{{page.component.title}}{{/if}}</span>
</div>